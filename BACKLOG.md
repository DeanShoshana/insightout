# BACKLOG

Items deferred to a named milestone or parked. SPEC.md §9 remains the milestone
plan; this file tracks enhancements raised in reviews so they aren't lost.

## M4 — Polish (in addition to SPEC §9 M4 scope)

- **Richer supporting motion** (raised at M2b review): the M2 motion base
  (hero cascade, scroll-linked hero exit, rise-on-enter reveals, nav frost) is
  intentionally restrained. M4 should deepen it within §6 discipline —
  candidates: animate the Engine Diagram speed-line gradient at the convergence
  moment, logo speed-lines micro-animation (the `<g id="speedlines">` rects are
  individually animatable by design), magnetic-ish CTA hover (§6 micro table),
  scrollspy on the Services sub-nav (§6, lands with M3 markup), staggered
  offering-line reveals inside cards. All: transforms+opacity only, one pinned
  scene max (the Engine scene), `prefers-reduced-motion` → final state.

## Parked (per SPEC)

- Advisory Circle section — flagged off until signed MOUs (§4).
- Case studies (/work) and Insights (/insights) — reserved, unlinked (§4).
