---
// Inner-page intro: kicker + h1 + lede, sharing the .section-head look.

interface Props {
  kicker: string;
  headline: string;
  lede?: string;
}

const { kicker, headline, lede } = Astro.props;
---

<header class="page-intro">
  <div class="container">
    <p class="section-head__kicker">{kicker}</p>
    <h1 class="page-intro__headline">{headline}</h1>
    {lede && <p class="page-intro__lede">{lede}</p>}
  </div>
</header>

<style>
  .page-intro {
    padding-block: clamp(3.5rem, 8vw, 7rem) clamp(2rem, 5vw, 3.5rem);
  }

  .page-intro__headline {
    font-size: clamp(2.25rem, 5vw, 3.5rem);
    max-inline-size: 18em;
    line-height: 1.08;
    margin-block-end: 1rem;
  }

  .page-intro__lede {
    font-size: clamp(1.1rem, 2vw, 1.3rem);
    line-height: 1.55;
    color: var(--graphite);
    max-inline-size: 44rem;
    margin: 0;
  }

  @media (max-width: 720px) {
    .page-intro {
      padding-block: clamp(2.75rem, 12vw, 4.25rem) clamp(1.5rem, 8vw, 2.5rem);
    }

    .page-intro__headline {
      font-size: clamp(1.9rem, 9vw, 2.55rem);
      line-height: 1.08;
      max-inline-size: 12.5em;
      margin-block-end: 0.75rem;
    }

    .page-intro__lede {
      font-size: 1rem;
      line-height: 1.55;
    }
  }
</style>
